{
  "name": "elm-tailwind-twitter-mock",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "elm-live src/Main.elm --output=dist/main.js --dir=dist --port=3000 --debug --pushstate",
    "build": "elm-make src/Main.elm --output=dist/main.js"
  }
}
